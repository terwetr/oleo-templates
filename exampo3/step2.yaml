title: Cobble a location
required:
  - repoUrl
properties:
  repoUrl:
    title: Repository Location Cobble
    type: string
    ui:field: RepoUrlPicker
    ui:options:
      allowedHosts:
        - github.com
